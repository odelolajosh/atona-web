import * as Tabs from '@radix-ui/react-tabs';

const Tab = Tabs.Root;
const TabList = Tabs.List;
const TabItem = Tabs.Trigger;
const TabContent = Tabs.Content;

export { Tab, TabList, TabItem, TabContent };
